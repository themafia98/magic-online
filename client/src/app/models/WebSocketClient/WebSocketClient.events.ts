export const socketErrorEvent = (error) => {
  console.error(error);
};

export const socketDisconnectEvent = (error) => {
  console.error(error);
};

export const socketError = (error) => {
  console.error(error);
};
